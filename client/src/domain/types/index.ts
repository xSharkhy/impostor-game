// Re-export types from shared package for convenience
export type {
  Room,
  Player,
  RoomStatus,
  ClientRoom,
  Category,
  Word,
  ErrorCode,
  ClientToServerEvents,
  ServerToClientEvents,
} from '@impostor/shared'

export { ErrorCodes, CONSTANTS } from '@impostor/shared'
